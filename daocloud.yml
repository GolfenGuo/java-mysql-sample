image: openjdk7

services:
  - mysql

env:
  - DB_HOST=mysql
  - DB_PORT=3306
  - DB_USERNAME=root
  - DB_NAME=test

script:
  - mysql -u root -h mysql -e 'create database if not exists test;'
  - mvn clean cobertura:cobertura
  - mvn test